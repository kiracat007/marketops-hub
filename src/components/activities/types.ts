export const activityTypes = ["Event", "Exhibition", "Field Demo", "Webinar", "Roadshow", "Product Launch", "Offline Promotion", "Other"] as const;
export const activityStatuses = ["Planning", "Upcoming", "In Progress", "Completed", "Cancelled"] as const;
export type ActivityType = (typeof activityTypes)[number]; export type ActivityStatus = (typeof activityStatuses)[number];
export type Activity = { id: string | number; campaignId?: string; partnerId?: string; name: string; type: ActivityType; campaign: string; partner: string; owner: string; location: string; startDate: string; endDate: string; status: ActivityStatus; attendees?: number; expectedAttendees?: number; actualAttendees?: number; budget: number; spend?: number; targetLeads?: number; notes?: string; createdAt?: string; updatedAt?: string; };
export type ActivityDraft = Pick<Activity, "campaignId" | "partnerId" | "name" | "type" | "campaign" | "partner" | "owner" | "location" | "startDate" | "endDate" | "status" | "budget" | "spend" | "targetLeads" | "notes"> & { attendees: number };
